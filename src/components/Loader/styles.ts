import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        position: "absolute",
        zIndex: 100,
        height: "100%",
        width: "100%",
        justifyContent: "center",
        alignItems: "center",
    },
});
